# lmcache-vllm
The driver for LMCache core to run in vLLM

## Installation from source

Use docker image `lmcache/vllm-openai:2025-04-18`

```
pip install -e.
pip install lmcache==0.2.0
pip install vllm==0.6.2
```

Or use the updated Dockerfile in docker

TODO: update vllm version to 0.8.5